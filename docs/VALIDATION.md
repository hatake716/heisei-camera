# 検証記録

実施日: 2026-09-05

## ローカルで確認した範囲

- `:app:assembleDebug`：成功。
- `:app:testDebugUnitTest`：21 件成功（共有 URL 13 件、日付と metadata 8 件）。
- `:app:lintDebug`：エラーなし。依存関係の更新通知などの警告あり。
- APK：`apksigner verify --verbose --print-certs` でデバッグ署名を確認。
- Android 15 / API 35 の専用エミュレーターでインストール・起動を確認。
- 公式 `pano=` 形式の検証用リンクを Android の共有 Intent から受け取り、API キー未設定の案内と選択状態を表示。
- Google 以外の URL は取り込まず、元の選択を維持。
- 位置情報を拒否してもクラッシュせず、許可方法を案内。
- エミュレーターの模擬 GPS で位置更新と精度表示を確認（東京駅付近、±5 m）。実測値ではない。
- 縦・横画面の表示と、画面回転による Activity 再生成後のパノラマ ID 維持を確認。

共有リンクの動作確認には実在画像とは無関係の検証用 ID を使用した。過去画像の取得成功として扱わない。実機への入力操作、インストールは行っていない。

## API キー設定後に確認が必要な範囲

Google の実画像表示、過去画像が共有リンクで保持されること、撮影年月の取得、Google SDK と実機センサーの方向一致、歩行時の挙動、実画像を表示した後のしおり保存・再表示は未検証。

API キーと課金設定は未提供である。現時点の APK は Google マップを開く導線とリンク受付・画面を確認する初期版であり、アプリ内で過去画像を表示できる完成版ではない。

metadata REST キーの Android 制限は、正しい識別情報による成功と誤った識別情報による拒否を実通信で確認する必要がある。詳細は [実現性と設計判断](FEASIBILITY.md) を参照。
